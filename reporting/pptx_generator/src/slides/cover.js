// src/slides/cover.js
// Cover slide builder - title + subtitle + client + date
// TODO Phase E: Implement full cover layout per identity rules

export default function buildCoverSlide(pres, id, data) {
  throw new Error("buildCoverSlide not yet implemented (Phase E)");
}
